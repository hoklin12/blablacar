import 'package:flutter/material.dart';

class FullScreenRideModal extends StatelessWidget {
  const FullScreenRideModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Full-Screen Modal"),
      ),
      body: Center(
        //
      ),
    );
  }
}
